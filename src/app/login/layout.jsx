export const metadata = {
  title: {
    default: "Iniciar sesión",
  },
  description: "Inicia sesión en Potro Mercado",
};

export default function LoginLayout({ children }) {
  return <div>{children}</div>;
}
