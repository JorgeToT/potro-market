export const metadata = {
  title: {
    default: "Registrate",
  },
  description: "Registrate en Potro Mercado",
};

export default function RegisterLayout({ children }) {
  return <div>{children}</div>;
}
